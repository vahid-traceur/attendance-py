# 🎯 Face Recognition Attendance System (Python)

سیستم حضور و غیاب مبتنی بر پردازش تصویر و تشخیص چهره با استفاده از Python و OpenCV.  
این پروژه برای محیط‌های آموزشی و سازمانی کوچک طراحی شده و قابلیت ثبت ورود و خروج افراد به‌صورت خودکار را دارد.

---

## 📦 ویژگی‌ها

- ثبت چهره افراد
- تشخیص چهره زنده از وب‌کم
- ثبت حضور و غیاب در فایل Excel
- داشبورد وب real-time بدون refresh
- گزارش روزانه / هفتگی / ماهانه
- خروجی Excel فرمت‌بندی‌شده
- اجرای کامل سیستم با یک دستور

---

## 🛠 پیش‌نیازها

### سیستم‌عامل
- Windows 10/11 یا Ubuntu 20+

### نرم‌افزارها
- Python 3.9 یا 3.10 (پیشنهادی)
- Git
- Webcam

---

## 📥 کلون پروژه

```bash
git clone https://github.com/your-username/attendance-py.git
cd attendance-py
```

## 🧪 ساخت محیط مجازی (Virtual Environment)

Windows
```bash
python -m venv venv
venv\Scripts\activate
```

Linux / maxOS
```bash
python3 -m venv venv
source venv/bin/activate
```

## 📦 نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

## 👤 ثبت چهره افراد

```bash
python capture_faces.py
```

## اجرا به صورت دستی

### 🧠 آموزش مدل

```bash
python train_faces.py
```
در صورت موفقیت این فایل‌ها ساخته می‌شوند:
models/face_model.yml
models/label_map.pkl

### ▶ تشخیص چهره و ثبت در Excel

```bash
python recognize_faces.py
```

## اجرا به صورت خودکار

```bash
python run_app.py
```